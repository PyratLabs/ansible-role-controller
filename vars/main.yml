---
# vars file for ansible-role-controller

controller_ansible_command_list:
  - ansible
  - ansible-connection
  - ansible-doc
  - ansible-inventory
  - ansible-playbook
  - ansible-vault
  - ansible-config
  - ansible-console
  - ansible-galaxy
  - ansible-lint
  - ansible-pull
